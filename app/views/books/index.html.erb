<p style="color: green"><%= notice %></p>

<% content_for :title, "Books" %>

<h1>Books</h1>

<div id="books">
  <% @books.each do |book| %>
    <%= render book %>
    <p>
      <%= link_to "Show this book", user_book_path %>
    </p>
  <% end %>
</div>

<%= link_to "New book", new_user_book_path %>
